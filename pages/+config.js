export { config };

import vikeReact from 'vike-react/config';
import { Layout } from './Layout';

const config = {
    Layout: Layout, // https://vike.dev/Layout
    extends: vikeReact, // https://vike.dev/extends
};
